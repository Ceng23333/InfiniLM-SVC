# Example environment file for InfiniLM-SVC Docker container
# Usage: docker run --env-file docker.env.example ...

# ============================================================================
# Component Launch Configuration
# ============================================================================
# LAUNCH_COMPONENTS: Which components to launch
#   Options:
#     - "all" (default): Launch registry, router, and babysitters
#     - "babysitter": Launch only babysitters (requires remote registry/router)
#     - Comma-separated: "registry,router" or "registry,babysitter", etc.
LAUNCH_COMPONENTS=all

# ============================================================================
# Port Configuration
# ============================================================================
# Ports for locally launched components
REGISTRY_PORT=18000
ROUTER_PORT=8000

# ============================================================================
# Remote Endpoint Configuration
# ============================================================================
# Use these when components are NOT launched locally (e.g., LAUNCH_COMPONENTS=babysitter)
# REGISTRY_URL: Remote registry endpoint (required if registry not launched locally)
# REGISTRY_URL=http://registry-host:18000

# ROUTER_URL: Remote router endpoint (optional, used by babysitters)
# ROUTER_URL=http://router-host:8000

# ROUTER_REGISTRY_URL: Registry URL for router to connect to
# (defaults to http://localhost:REGISTRY_PORT if registry launched locally,
#  or REGISTRY_URL if registry not launched locally)
# ROUTER_REGISTRY_URL=http://remote-registry:18000

# ============================================================================
# Babysitter Configuration
# ============================================================================
# BABYSITTER_CONFIGS: Space-separated list of babysitter config file paths
# Paths can be relative to config/ directory or absolute paths
BABYSITTER_CONFIGS=config/babysitter1.toml config/babysitter2.toml

# ============================================================================
# Build Configuration
# ============================================================================
# BUILD_ON_STARTUP: Build Rust binaries on container startup (default: false)
# Set to "true" if binaries are not pre-built in the image
BUILD_ON_STARTUP=false
