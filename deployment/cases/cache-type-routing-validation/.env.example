# Environment variables for InfiniLM-SVC cache-type-routing-validation
# 
# Copy this file to .env and customize:
#   cp .env.example .env
#   # Edit .env with your values
#
# Usage:
#   source .env
#   ./start-master.sh <REGISTRY_IP>

# Docker image and container
IMAGE_NAME=infinilm-svc:infinilm-demo
CONTAINER_NAME=infinilm-svc-master

# Deployment case
DEPLOYMENT_CASE=cache-type-routing-validation

# Launch components
LAUNCH_COMPONENTS=all

# Ports
REGISTRY_PORT=18000
ROUTER_PORT=8000

# Babysitter configs (both paged and static cache instances)
BABYSITTER_CONFIGS=paged-cache-9g_8b_thinking.toml static-cache-9g_8b_thinking.toml

# Required model path (MUST be set - uncomment and set your path)
MODEL1_DIR=/data-aisoft/zenghua/models/9g_8b_thinking_llama

# Optional cache type routing threshold (optional)
# CACHE_TYPE_ROUTING_THRESHOLD=1000

# Optional directories (use container defaults if not set)
# INFINILM_DIR=/path/to/InfiniLM
# INFINICORE_DIR=/path/to/InfiniCore
# CONFIG_DIR=/path/to/config

# Proxy settings (optional)
# NO_PROXY=localhost,127.0.0.1,0.0.0.0
