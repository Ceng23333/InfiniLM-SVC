# Environment variables for InfiniLM-SVC integration-validation (Server 2)
# 
# Copy this file to .env.server2 and customize:
#   cp .env.server2.example .env.server2
#   # Edit .env.server2 with your values
#
# Usage:
#   source .env.server2
#   ./start-server2.sh <SERVER1_IP> <SERVER2_IP>

# Docker image
IMAGE_NAME=infinilm-svc:demo

# Network mode
USE_HOST_NETWORK=true

# Launch components (server 2 only runs babysitters)
LAUNCH_COMPONENTS=babysitter

# Babysitter configs
BABYSITTER_CONFIGS=babysitter-c.toml babysitter-d.toml

# Note: REGISTRY_URL, ROUTER_URL, BABYSITTER_HOST are set automatically
# based on SERVER1_IP and SERVER2_IP arguments
