# Environment variables for InfiniLM-SVC infinilm-metax-deployment (Master)
# 
# Copy this file to .env and customize:
#   cp .env.example .env
#   # Edit .env with your values
#
# Usage:
#   source .env
#   ./start-master.sh <REGISTRY_IP>

# Docker image and container
IMAGE_NAME=infinilm-svc:infinilm-demo
CONTAINER_NAME=infinilm-svc-master

# Deployment case
DEPLOYMENT_CASE=infinilm-metax-deployment

# Launch components
LAUNCH_COMPONENTS=all

# Ports
REGISTRY_PORT=18000
ROUTER_PORT=8000
EMBEDDING_PORT=20002

# Babysitter configs (will include embeddings if EMBEDDING_MODEL_DIR is set)
BABYSITTER_CONFIGS=master-9g_8b_thinking.toml master-Qwen3-32B.toml

# Required model paths (MUST be set - uncomment and set your paths)
MODEL1_DIR=/data-aisoft/zenghua/models/9g_8b_thinking_llama
MODEL2_DIR=/data-aisoft/zenghua/models/Qwen3-32B

# Optional model paths (for embedding server)
# EMBEDDING_MODEL_DIR=/path/to/MiniCPM-Embedding-Light
# RERANK_MODEL_DIR=/path/to/MiniCPM-Reranker-Light
# BCE_RERANK_MODEL_DIR=/path/to/bce-reranker-base_v1

# Optional directories (use container defaults if not set)
# INFINILM_DIR=/path/to/InfiniLM
# INFINICORE_DIR=/path/to/InfiniCore
# CONFIG_DIR=/path/to/config

# Proxy settings (optional)
# NO_PROXY=localhost,127.0.0.1,0.0.0.0
