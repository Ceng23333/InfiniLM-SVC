# Environment variables for InfiniLM-SVC infinilm-metax-deployment (Slave)
# 
# Copy this file to .env.slave and customize:
#   cp .env.slave.example .env.slave
#   # Edit .env.slave with your values
#
# Usage:
#   source .env.slave
#   ./start-slave.sh <MASTER_IP> <SLAVE_IP> [SLAVE_ID]

# Docker image and container
IMAGE_NAME=infinilm-svc:infinilm-demo
# CONTAINER_NAME will be auto-generated as infinilm-svc-slave, infinilm-svc-slave2, etc.

# Deployment case
DEPLOYMENT_CASE=infinilm-metax-deployment

# Launch components (slave only runs babysitter)
LAUNCH_COMPONENTS=babysitter

# Ports (for connecting to master)
REGISTRY_PORT=18000
ROUTER_PORT=8000

# Required model path (MUST be set - uncomment and set your path)
MODEL2_GGUF=/data-aisoft/zenghua/models/Qwen3-32B

# Optional directories (use container defaults if not set)
# INFINILM_DIR=/path/to/InfiniLM
# INFINICORE_DIR=/path/to/InfiniCore
# CONFIG_DIR=/path/to/config
